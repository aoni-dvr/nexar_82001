menu "Pmic Driver"

config ENABLE_PMIC
        bool "enable pmic"
        default n

if ENABLE_PMIC
config PMIC_AMBA_PWC
	bool "PMIC_AMBA_PWC"
	default n

config PMIC_CHIPON
	bool "PMIC_CHIPON"
	default n
endif
endmenu

