rtos_dram_asm_converter

usage: rtos_dram_asm_converter [-h] -m MODE -p PLATFORM -o [OUTPUT] [-t TEMPLATE] [-v]
                               [-d]
                               file [file ...]

positional arguments:
  file                  Specify file(s)

optional arguments:
  -h, --help            show this help message and exit
  -m MODE, --mode MODE  Mode (0: asm->ini, 1: h->asm)
  -p PLATFORM, --platform PLATFORM
                        Specify platform [CV2/CV22/CV25/CV28/CV5/CV3]
  -o [OUTPUT], --output [OUTPUT]
                        Output file
  -t TEMPLATE, --template TEMPLATE
                        Specify asm template file for h->asm substitution
  -v, --version         Show version
  -d, --debug           Enable more debug messages



Example:
1. To translate .ini file from existing .asm file
rtos_dram_asm_converter -m 0 -p cv5 -o test.ini AmbaLPDDR5_2496Mhz.asm
2. To translate .asm file from .h file generated by preshmoo tool (bstiniparser)
rtos_dram_asm_converter -m 1 -p cv5 -o test.asm test.h



Binary package command:
---------- Tested with Python 3.7.10 and pyinstaller 5.6.2 ----------
(Linux)     pyinstaller --hiddenimport bitarray --add-data "lib/*:lib" --one-file rtos_dram_asm_converter.py
(Windows)   pyinstaller.exe --hiddenimport bitarray --add-data "lib\*;lib" rtos_dram_asm_converter.py

Note: For Linux, check the GLIBC lib version when packing with pyinstaller!
      If the binary is packed with higher version GLIBC, but being executed in older system, new GLIBC could be not found....


Prebuilt binary:
bin/linux_x86_64
bin/win_x86_64

