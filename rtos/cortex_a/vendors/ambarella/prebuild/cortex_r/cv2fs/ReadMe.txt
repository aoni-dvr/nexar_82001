This folder is for prebuild image on Cortex_R52.
Default dram clock setting is 1800Mhz.

* For amba_bst_xxx.bin, xxx is dram frequency.
* You could directly replace the amba_bst.bin with the amba_bst_xxx.bin to use specified dram frequency. 
